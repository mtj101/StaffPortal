@{
    Layout = null;
    string route = ((System.Web.Routing.Route)ViewContext.RouteData.Route).Url;
}
<aside class="main-sidebar">
    <!-- sidebar: style can be found in sidebar.less -->
    <section class="sidebar">
        <!-- search form -->
        <form action="#" method="get" class="sidebar-form">
            <div class="input-group">
                <input type="text" name="q" class="form-control" placeholder="Search..." />
                <span class="input-group-btn">
                    <button type='submit' name='search' id='search-btn' class="btn btn-flat"><i class="fa fa-search"></i></button>
                </span>
            </div>
        </form>
        <!-- /.search form -->
        <!-- sidebar menu: : style can be found in sidebar.less -->
        <ul class="sidebar-menu">
            <li class="header">MAIN NAVIGATION</li>
            <li class="@(route == "staff" ? "active" : "")">
                <a href="@Url.Action("Index", "Home")">
                    <i class="fa fa-home"></i> <span>Home</span>
                </a>
            </li>
            <li class="@(route == "calendar" ? "active" : "")">
                <a href="@Url.Action("Calendar", "Home")">
                    <i class="fa fa-calendar"></i> <span>Calendar</span>
                    <small class="label pull-right bg-red">3</small>
                </a>
            </li>
            @if (User.Identity.IsAuthenticated)
            {
                <li class="@(route == "userpanel" ? "active" : "")">
                    <a href="@Url.Action("UserPanel", "Home")">
                        <i class="fa fa-user"></i> <span>User Panel</span>
                    </a>
                </li>
            }
            @if (User.IsInRole("Admin") || User.IsInRole("Supervisor"))
            {
                <li class="header">SUPERVISOR TOOLS</li>
                <li class="treeview">
                    <a href="#">
                        <i class="fa fa-users"></i> <span>Supervisor options</span>
                        <i class="fa fa-angle-left pull-right"></i>
                    </a>
                    <ul class="treeview-menu">
                        <li>
                            <a href="@Url.Action("AddNewUser", "Admin")">
                                <i class="fa fa-calendar-check-o"></i> Requests Pending
                                <small class="label pull-right bg-red">2</small>
                            </a>
                        </li>
                        <li>
                            <a href="pages/forms/advanced.html"><i class="fa fa-pencil-square-o"></i> Edit Existing</a>
                        </li>
                    </ul>
                </li>
            }
            @if (User.IsInRole("Admin"))
            {
                <li class="header">ADMIN TOOLS</li>
                <li class="treeview @(route.StartsWith("admin") ? "active" : "")">
                    <a href="#">
                        <i class="fa fa-users"></i> <span>Manage Users</span>
                        <i class="fa fa-angle-left pull-right"></i>
                    </a>
                    <ul class="treeview-menu">
                        <li class="@(route == "admin/adduser" ? "active" : "")"><a href="@Url.Action("AddNewUser", "Admin")"><i class="fa fa-plus-square-o"></i> Add New</a></li>
                        <li><a href="pages/forms/advanced.html"><i class="fa fa-pencil-square-o"></i> Edit Existing</a></li>
                    </ul>
                </li>
            }
        </ul>
    </section>
    <!-- /.sidebar -->
</aside>
